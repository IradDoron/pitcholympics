import { Link } from '@core';

const PostPage = () => {
    return (
        <div>
            full post
            <Link label='back to library' url='/library/library/posts' />
        </div>
    );
};

export default PostPage;
