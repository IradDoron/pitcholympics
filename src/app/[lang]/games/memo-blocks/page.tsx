import { MemoBlocksGame } from '@shared';

const Page = async () => {
    return (
        <div className='h-full flex justify-center items-center'>
            <MemoBlocksGame />
        </div>
    );
};

export default Page;
