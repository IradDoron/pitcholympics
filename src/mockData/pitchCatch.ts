import { PitchCatchGame } from '@/types';

const pitchCatchData: PitchCatchGame = [
  [
    [{ pitch: [4], options: [[4], [2], [5]] }],
    [{ pitch: [4], options: [[4], [5], [2]] }],
  ],
];

export default pitchCatchData;
